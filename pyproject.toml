[project]
name = "analisis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["pandas", "matplotlib", "seaborn", "plotly", "nbformat"]

[project.optional-dependencies]
dev = ["pre-commit"]
